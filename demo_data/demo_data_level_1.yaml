---
# Insert further demo data that contains foreign keys to other tables,
# linking additional records.
person:
    new1:
        given_name: Harry
        surname: Kim
        unix_account: ensign4ever
        id_organization: {idByList(db.conn = db.conn, l = list(shorthand = 'we'), tab = 'organization')}

spot:
    new1:
        name: garden 1
        description: under the roof of the bike stand
    new2:
        name: childrens room 1
        description: left to the bed

collection:
    new1:
        id_matrix: {idByList(db.conn = db.conn, l = list(name = 'outside air'), tab = 'matrix')}
        collection_class: 'time series'
        id_spot: {idByList(db.conn = db.conn, l = list(name = 'garden 1'), tab = 'spot')}
    new2:
        id_matrix: {idByList(db.conn = db.conn, l = list(name = 'indoor air'), tab = 'matrix')}
        collection_class: 'time series'
        id_spot: {idByList(db.conn = db.conn, l = list(name = 'childrens room 1'), tab = 'spot')}
---