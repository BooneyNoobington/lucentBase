---
# Insert further demo data that contains foreign keys to other tables,
# linking additional records.
person:
    new1:
        given_name: Harry
        surname: Kim
        unix_account: ensign4ever
        id_organization: {idByList(list(shorthand = 'we'), 'organization')}

spot:
    new1:
        name: garden 1
        description: under the roof of the bike stand

collection:
    new1:
        name: {paste(getNextIdentifier(), collapse = '-')}
        id_matrix: {idByList(list(name = 'outside air'), 'matrix')}
        collection_class: 'time series'
        id_spot: {idByList(list(name = 'garde 1'), 'spot')}

---